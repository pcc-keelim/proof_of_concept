SET allow_introspection_functions=1;
CREATE USER IF NOT EXISTS admin_marc IDENTIFIED BY 'CHANGE_ME_AFTER_DEPLOYMENT';
GRANT ALL ON *.* TO admin_marc WITH GRANT OPTION;
SET allow_introspection_functions=0;
CREATE USER IF NOT EXISTS airbyte_user IDENTIFIED BY 'CHANGE_ME_AFTER_DEPLOYMENT';
CREATE USER IF NOT EXISTS prod_dagster IDENTIFIED BY 'CHANGE_ME_AFTER_DEPLOYMENT';
CREATE USER IF NOT EXISTS prod_dbt_user IDENTIFIED BY 'CHANGE_ME_AFTER_DEPLOYMENT';
CREATE USER IF NOT EXISTS stage_dbt_user IDENTIFIED BY 'CHANGE_ME_AFTER_DEPLOYMENT';
CREATE USER IF NOT EXISTS clickhousebackup IDENTIFIED BY 'CHANGE_ME_AFTER_DEPLOYMENT';
GRANT SELECT, ALTER FREEZE PARTITION, DROP FUNCTION, CREATE FUNCTION, CREATE DATABASE, CREATE TABLE, INSERT, DROP TABLE ON *.* TO clickhousebackup;